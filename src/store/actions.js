export const addHero = (hero) => ({ type: 'ADD_HERO', hero });

export const removeHero = (hero) => ({ type: 'REMOVE_HERO', hero });

export const changeText = (text) => ({ type: 'CHANGE_TEXT', text });
